RSpec.describe Jekyll::Server::Rendered::Tweet do
  it "has a version number" do
    expect(Jekyll::Server::Rendered::Tweet::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
